services:
  splitpro:
    image: splitpro-local:latest
    build:
      context: ../..
      dockerfile: Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-selfhost}
        GIT_SHA: ${GIT_SHA:-local}
